我们的训练集数据包括 DIV2K[]、Flickr2K[]、DIV8K[]和来自 FFHQ[]的 10,000 张人脸图像。我将以上数据集的图片裁剪不重叠的512×512补丁，并使用我们采用与 BSRGAN [] 相同的退化模型来生成 LR 补丁。此外，我们还最终的训练数据集由 198,061 个补丁组成。

我们使用以下方式生成训练补丁：（1）裁剪不重叠的512×512补丁； (2) 过滤纹理较少的补丁； (3) 对于 FFHQ 中对齐良好的面部，我们在裁剪之前以 [0.5, 1.0] 之间的比例因子执行随机调整大小，以避免内容偏差。补充材料中提供了更多详细信息。最终的训练数据集包含 136,205 个大小为 512 × 512 的 HR 块。我们使用与 BSRGAN3 相同的退化模型来生成相应的 LR 图像。综合测试数据集 为了确保公平比较，我们使用最近两个作品 BSRGAN 和 Real-ESRGAN 的混合退化模型（记为 bsrgan_plus5）来生成 DIV2K 验证集和 5 个经典基准测试集（即 Set5、Set14、BSD100）的 LR 测试集， Urban100 和 Manga109。测试图像的多样性保证了模型性能的全面评估。真实世界测试数据集 我们在三个最近的真实世界基准测试上测试我们的模型，包括 RealSR [44]、DRealSR [47] 和 DPED-iphone [18]。我们针对这些真实世界的数据集以 4 倍的放大倍数来测试模型。 RealSR 和 DRealSR 的图像由 DSLR 相机捕获，分别包含 100 和 93 个图像。 DPED-iphone 包含由智能手机摄像头拍摄的 100 张 LR 图像。 DPED-iphone 中的 LR 图像通常比 RealSR 和 DRealSR 中的图像更损坏。评估指标对于具有地面真实图像的合成测试数据集，我们采用众所周知的感知指标LPIPS [56]分数来评估生成图像的感知质量。我们还报告了广泛使用的 PSNR、SSIM 分数的结果以供参考。对于现实世界的基准，通常没有真实图像，因此我们采用众所周知的无参考指标 NIQE 分数进行定量比较。


生成培训 HR 补丁。如论文中所述，我们使用来自 DIV2K [1]、Flickr2K [29]、DIV8K [15] 的高分辨率图像和来自 FFHQ [21] 的 10,000 张人脸图像来生成大小为 512 × 512 的 HR 训练块。训练图像如表 1 所示。 5，一些例子如图10所示。获取补丁的详细信息如下：对于包含自然图像的前三个数据集，我们按照以下步骤裁剪补丁：（1）裁剪不重叠的512×512补丁； (2) 过滤纹理（或边缘）很少的补丁。为此，我们首先计算补丁的索贝尔边缘图，然后计算边缘图的平均值和变化，表示为μ，σ2。由于边缘图稀疏，更多边缘意味着更大的 σ2 和 μ，因此我们凭经验过滤 σ2 < 10 的 patch；对于 FFHQ 人脸数据集，图像是尺寸为 1024 × 1024 且对齐良好的人脸。之前的非重叠裁剪会导致该数据集出现内容偏差。因此，我们首先以 [0.5, 1.0] 之间的比例因子随机调整图像大小，然后从每张图像中仅随机裁剪一个补丁。在线生成训练对。我们使用与 BSRGAN [53] 相同的退化模型在线生成相应的 LR 图像。具体来说，输入的HR补丁首先被随机裁剪为256×256补丁，然后使用degradation_bsrgan4函数以比例因子2和4进行降级以生成训练对。生成综合测试基准。为了公平比较，我们使用 BSRGAN 和 RealESRGAN 的混合退化模型来合成测试 LR 图像。具体来说，我们使用degradation_bsrgan_plus5 函数具有比例因子 2 和 4，用于生成具有固定随机种子 123 的测试对。